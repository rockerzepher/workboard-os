# Contributing

Create a scoped branch with the `codex/` prefix. Keep changes small and explain any product decision that changes source-of-truth behavior.

Before handoff, run `pnpm lint`, `pnpm test`, and `pnpm build`. Use synthetic fixtures and dry-run import behavior before adding provider writes. Do not add Google, Obsidian, or Notion credentials to the repository.
